#include <stdio.h>
#define MAX 100
int main() {
    int i,n,item,arr[MAX];
    printf("Enter no. of elements\n");
    scanf("%d",&n);
    for(i=0;i<n;i++){
    printf("enter element %d:\n",i+1);
    scanf("%d",&arr[i]);}
    int j = 0,temp;
    for (i = 0; i < n; i++)
    {
        if (arr[i] < 0)//non-zero
        {
            if (i != j)
            {
            temp=arr[i];
            arr[i]=arr[j];
            arr[j]=temp;
            }
            j++;
        }
    }
    for(i=0;i<n;i++){
    printf("%d ",arr[i]);
  }
  return 0;
}